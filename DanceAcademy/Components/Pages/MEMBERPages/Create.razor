@page "/members/create"
@rendermode InteractiveServer
@using Microsoft.EntityFrameworkCore
@using DataAccess
@inject IDbContextFactory<DanceAcademy.Data.DanceAcademyContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Add New Member - Dance Academy</PageTitle>

<div class="page-header">
    <h1><i class="bi bi-person-plus-fill me-2"></i>Add New Member</h1>
    <p>Register a new member to the dance academy</p>
</div>

<EditForm Model="Member" OnValidSubmit="AddMember" FormName="CreateMember">
    <DataAnnotationsValidator />
    <ValidationSummary class="alert alert-danger" role="alert"/>
    
    <div class="form-section">
        <h3 class="form-section-title"><i class="bi bi-person me-2"></i>Personal Information</h3>
        <div class="form-row">
            <div class="mb-3">
                <label for="member_first_name" class="form-label">First Name</label>
                <InputText id="member_first_name" @bind-Value="Member.member_first_name" class="form-control" placeholder="Enter first name" />
                <ValidationMessage For="() => Member.member_first_name" class="text-danger" />
            </div>
            <div class="mb-3">
                <label for="member_last_name" class="form-label">Last Name</label>
                <InputText id="member_last_name" @bind-Value="Member.member_last_name" class="form-control" placeholder="Enter last name" />
                <ValidationMessage For="() => Member.member_last_name" class="text-danger" />
            </div>
        </div>
        <div class="form-row">
            <div class="mb-3">
                <label for="date_of_birth" class="form-label">Date of Birth</label>
                <InputDate id="date_of_birth" @bind-Value="Member.date_of_birth" class="form-control" />
                <ValidationMessage For="() => Member.date_of_birth" class="text-danger" />
            </div>
            <div class="mb-3">
                <label for="member_gender" class="form-label">Gender</label>
                <InputSelect id="member_gender" @bind-Value="Member.member_gender" class="form-control">
                    <option value="">Select gender...</option>
                    <option value="M">Male</option>
                    <option value="F">Female</option>
                    <option value="Other">Other</option>
                </InputSelect>
                <ValidationMessage For="() => Member.member_gender" class="text-danger" />
            </div>
        </div>
    </div>

    <div class="form-section">
        <h3 class="form-section-title"><i class="bi bi-envelope me-2"></i>Contact Information</h3>
        <div class="form-row">
            <div class="mb-3">
                <label for="member_email" class="form-label">Email Address</label>
                <InputText id="member_email" @bind-Value="Member.member_email" class="form-control" placeholder="email@example.com" type="email" />
                <ValidationMessage For="() => Member.member_email" class="text-danger" />
            </div>
            <div class="mb-3">
                <label for="member_phone_number" class="form-label">Phone Number</label>
                <InputText id="member_phone_number" @bind-Value="Member.member_phone_number" class="form-control" placeholder="+1 (555) 123-4567" />
                <ValidationMessage For="() => Member.member_phone_number" class="text-danger" />
            </div>
        </div>
        <div class="mb-3">
            <label for="member_address" class="form-label">Address</label>
            <InputText id="member_address" @bind-Value="Member.member_address" class="form-control" placeholder="Enter full address" />
            <ValidationMessage For="() => Member.member_address" class="text-danger" />
        </div>
    </div>

    <div class="form-section">
        <h3 class="form-section-title"><i class="bi bi-calendar-check me-2"></i>Membership</h3>
        <div class="form-row">
            <div class="mb-3">
                <label for="member_join_date" class="form-label">Join Date</label>
                <InputDate id="member_join_date" @bind-Value="Member.member_join_date" class="form-control" />
                <ValidationMessage For="() => Member.member_join_date" class="text-danger" />
            </div>
        </div>
    </div>

    <div class="form-actions">
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-lg"></i> Create Member
        </button>
        <a href="/members" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>
    </div>
</EditForm>

@code {
    private MEMBER Member { get; set; } = new() 
    { 
        date_of_birth = DateTime.Today.AddYears(-18),
        member_join_date = DateTime.Today 
    };

    private async Task AddMember()
    {
        using var context = DbFactory.CreateDbContext();
        context.MEMBER.Add(Member);
        await context.SaveChangesAsync();
        NavigationManager.NavigateTo("/members");
    }
}
